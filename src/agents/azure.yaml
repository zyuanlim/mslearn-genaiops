# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mslearn-genaiops
metadata:
  template: mslearn-genaiops@0.0.1-beta

services:
  agent:
    project: .
    language: python
    host: foundry
